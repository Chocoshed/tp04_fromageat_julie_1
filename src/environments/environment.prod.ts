/**
 * Environnement de production
 * Utilisé lors du `ng build` avec la configuration de production
 */
export const environment = {
  production: true,
  apiUrl: 'https://tp-4-latest.onrender.com/api'
};
